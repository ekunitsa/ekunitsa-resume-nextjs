import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
    footer: {
        position: 'absolute',
        left: 42,
        right: 42,
        top: 800,
        color: '#475467',
        fontSize: 8,
        textAlign: 'right',
    },
});
