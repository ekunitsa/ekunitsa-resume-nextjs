import { StyleSheet } from '@react-pdf/renderer';

export const styles = StyleSheet.create({
    page: {
        padding: 42,
        color: '#000000',
        fontFamily: 'Montserrat',
        fontSize: 12,
        lineHeight: 1.5,
    },
    name: {
        fontSize: 24,
        fontWeight: 600,
        lineHeight: 0.9,
    },
    role: {
        marginTop: 12,
        color: '#0d75c4',
        fontSize: 16,
        fontWeight: 500,
    },
});
